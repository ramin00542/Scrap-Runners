# Task 12-02 — Export preset setup

## ID
TASK-12-02

## Objective
Document how to configure an export preset locally. `export_presets.cfg`
remains gitignored (machine-specific paths).

## Allowed Files
- AI_DOCS/EXPORT_SETUP.md

## Requirements
Document, for a Desktop export preset:
1. Required Godot export templates matching the locked engine version.
2. Steps to create the preset via Project > Export.
3. Recommended settings consistent with 05_TECH_SPEC.md.
4. Note that `export_presets.cfg` is intentionally not committed.

## Acceptance Criteria
- [ ] Following the documented steps produces a runnable exported build.

## Test Procedure
1. Follow `EXPORT_SETUP.md` on a real machine, confirm a working export.

## Required Report Format
Implementation Mode.
