Create HealthComponent (Node) with: signals health_changed(current,max) and died(), @export max_health, current_health, initialize(maximum_health) for dynamic setup, take_damage(amount), heal(amount), is_dead(). Create health_component_test.tscn with tests: starts at max, damage reduces, no-op on 0/negative, heal works and clamps, damage after death ignored, initialize ordering fix. AC: ALL PASS.