Create SaveManager autoload (no class_name): save_stash(stash, path) using ResourceSaver.save, load_stash(path) using ResourceLoader.load with CACHE_MODE_IGNORE, handles missing file (returns empty) and corrupted (returns empty + warning). Test uses SEPARATE path never touching real save. 3 tests: round-trip, corrupted file, missing file. AC: 3 PASS lines, real save file never touched.