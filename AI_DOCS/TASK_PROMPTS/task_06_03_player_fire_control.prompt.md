Add fire control to player_controller.gd: @export equipped_weapon preloading scrap_pistol.tres, _fire_cooldown var, _process calls _process_combat(delta) which checks attack input + cooldown. _fire() instantiates projectile at player pos, sets damage and direction toward mouse. CRITICAL: establish _process_xxx() composition convention — never replace _process() wholesale. AC: Fires toward cursor, respects 0.35s fire rate.