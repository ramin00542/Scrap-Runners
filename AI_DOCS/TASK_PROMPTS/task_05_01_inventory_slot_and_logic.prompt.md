Create InventorySlot (RefCounted, item:ItemData, amount:int, is_empty()) and Inventory (Node, @export slot_count=16, _slots array resized in _ready). Implement add_item (stack-aware, fills existing then new slots, emits inventory_changed, returns leftover), remove_item (by item_id), has_item, get_slots(). Create inventory_test.tscn with tests on 4-slot inventory: partial fill, exact stack, overflow to new slot, null/0/-3 no-ops, signal not emitted for no-ops, removal clears slots. AC: ALL PASS.