Add patrol state to enemy_controller: enum State {PATROL,CHASE,ATTACK}, @export NodePath patrol_point_a/b, cache Marker2D world positions in _ready (important — Marker2D children move with parent), _process_patrol walks between points. Add two Marker2D children to scene, wire NodePaths. CRITICAL: always set velocity after switching target (fix overshoot bug). AC: Enemy walks between FIXED world positions.