Create GameManager autoload (no class_name): go_to_scene(path) using change_scene_to_file. Create raid_door.gd for Hub→Raid transition via _process (not single-frame body_entered). Update test_level.gd: CUMULATIVE _ready keeping all signals, add extraction_completed→stash transfer, add player_died→return to Hub. Set Hub as main scene in project.godot. AC: Full loop works (Hub→Raid→extract→stash→Hub), death returns to Hub with stash unchanged.