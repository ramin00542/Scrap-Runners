Create test_level.gd signals for extraction: _ready connects extraction_point signals (started/progressed/cancelled/completed). Add ExtractionBar ProgressBar to Player CanvasLayer (visible=false, min=0, max=1). In player_controller: show_extraction_bar(bool), update_extraction_progress(float). test_level methods call these. AC: Bar fills LIVE during extraction, hides on cancel/complete.