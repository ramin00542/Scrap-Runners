# گزارش اصلاحات دوم — مشکلات 16 گانه — 2026-07-21

این گزارش بر اساس فایل `مشکل دیگه هست.md` تهیه شده و بدون ساخت کد انجام شد (فقط MD).

## فهرست مشکلات و اصلاحات

### 1. فرایند تغییر CURRENT_TASK.md مجوز نداشت
**فیکس:** `01_RULES.md` بخش 6.5 جدید اضافه شد:
- `CURRENT_TASK.md` هم مثل CHANGELOG از Allowed Files مستثناست، اما فقط پس از:
  1. تایید Acceptance Criteria به روش 6.4
  2. ثبت Changelog
  3. دستور مستقیم کاربر یا کامنت footer در Task فایل
- AI باید انتقال را گزارش کند: "CURRENT_TASK transitioned from TASK-XX to TASK-YY"

### 2. Physics Consistency هنوز اشتباه بود
**فیکس:** قبلاً در گزارش اول انجام شد، ولی `task_01_04` دوباره کاملاً بازنویسی شد با مقادیر bitmask 1,2,4,8,16,32,64 و ارجاع به ADR-014 (ADR-013 superseded).

### 3. TASK-03-03 منطق Animation را حذف می‌کرد
**فیکس:**
- `task_02_04` بازنویسی شد تا از ابتدا از helper methods `_process_movement()` و `_update_movement_animation()` استفاده کند.
- `task_03_03` بازنویسی شد تا ساختار حفظ شود:
  ```gdscript
  _physics_process:
    if _is_dead: _process_dead_movement(); return
    _process_movement()
    _update_movement_animation()
  ```
- انیمیشن idle بعد مرگ حفظ می‌شود، velocity صفر می‌شود.

### 4. Projectile با دیوار حذف نمی‌شد
**فیکس:** `task_06_02/projectile.gd`:
- قبل: فقط اگر health داشت queue_free می‌کرد → از دیوار عبور می‌کرد
- بعد: همیشه بعد برخورد queue_free، چه health داشته باشد چه دیوار باشد
- Type annotation اضافه: `as HealthComponent`

### 5. `body.inventory` خطای Type Safety
**فیکس:** `task_09_02/loot_pickup.gd`:
- قبل: `body.inventory` روی `Node2D` که فیلد ندارد
- بعد:
  ```gdscript
  if not body is PlayerController: return
  var player: PlayerController = body as PlayerController
  _player_inventory = player.inventory
  ```
  همین چک در `_on_body_exited` هم اضافه شد.

### 6. تست Lambda متغیر محلی را تغییر می‌داد
**فیکس:**
- `task_03_01/health_component_test.gd`: حذف lambda `func(_c,_m): changed_count+=1`
  جایگزین با member vars `_changed_count`, `_died_count` و متدهای `_on_health_changed(current:float, max:float)` و `_on_died()` با Type Annotation
- `task_05_01/inventory_test.gd`: حذف lambda برای `signal_count_before`
  جایگزین با `_signal_count_before` member و `_on_inventory_changed_for_noop_test()`

### 7. قانون تست در همان Task نقض شده بود
لیست: 06-03, 07-03, 07-04, 07-05, 09-04, 10-00
**فیکس:** برای هرکدام در Requirements توضیح صریح exemption اضافه شد per rule 10 clause "unless the task file explicitly says otherwise":
- 06-03: verification via manual + committed test in 06-04/06-05
- 07-03: patrol fixed points manual, full cycle log in 07-06
- 07-04: chase manual + 07-06
- 07-05: attack + enemy_died in 07-06
- 09-04: extraction signals via prints inside Allowed File + integration in 09-05/09-07
- 10-00: ItemDatabase lookup via SaveManager tests in 10-03/10-04

این کار rule 10 را رعایت می‌کند بدون نیاز به کد جدید.

### 8. ساخت PNG با AI متنی قابل اتکا نیست
**فیکس:**
- `task_02_01`, `task_07_02`, `task_09_01`, `task_09_02` همه از PNG به Polygon2D تغییر یافتند
- Allowed Files: حذف `.png` و `08_ASSET_LICENSES.md`
- Requirements: Polygon2D با رنگ مشخص (سبز player, قرمز enemy, زرد diamond loot, خاکستری floor)
- `04_ART_DIRECTION.md`: تاکید شد Polygon2D برای MVP اجباری است، PNG فقط برای final art، و نیاز به license برای Polygon2D نیست
- این باعث حذف وابستگی به تولید فایل باینری توسط AI متنی می‌شود.

### 9. Rule کامنت Collision در Scene قابل اجرا نبود
**فیکس:** `01_RULES.md` بخش 9 اصلاح شد:
- اگر مقدار در GDScript ست می‌شود: باید کامنت `per ADR-014` داشته باشد
- اگر مقدار در `.tscn` Inspector ست می‌شود: کامنت داخل `.tscn` ممکن نیست، نیاز با مستندسازی صریح در Task file و Test Procedure که چک‌باکس‌ها را چک می‌کند برآورده می‌شود
- ترجیح: ست کردن در کد برای auditability، ولی Inspector هم وقتی در Task مستند شود مجاز است
- پیشنهاد استفاده از `1 << n` برای وضوح

### 10. TASK-09-03 قابل اجرای تکرارپذیر نبود (Allowed Files: None)
**فیکس:** `task_09_03_pickup_interaction.md` کاملاً بازنویسی شد:
- Allowed Files اکنون `loot_pickup_test.tscn`, `loot_pickup_test.gd`
- سناریوی partial pickup (inventory 55/60, pickup 20 → leftover 15) تعریف شد
- هم تست committed (ALL PASS) و هم تست دستی

### 11. Patrol نوسان/overshoot نزدیک نقطه
**فیکس:** `task_07_03`:
- قبل: اگر distance < 4.0 → toggle target و `return` بدون ست velocity → سرعت فریم قبلی باقی می‌ماند
- بعد:
  ```gdscript
  if length < 4.0:
    toggle
    direction = new_target - pos
  if direction.length > 0.1:
    velocity = normalized * speed
  else velocity = ZERO
  ```
  همیشه velocity ست می‌شود، return حذف شد.

### 12. ItemDatabase Dictionary تایپ‌شده نبود
**فیکس:** `task_10_00`:
- `Dictionary` → `Dictionary[StringName, ItemData]`
- حلقه با Type Hint: `for item: ItemData in all_items`
- چک item_id خالی و duplicate با push_warning

### 13. نام Task مربوط به GameManager ناسازگار بود
**فیکس:** قبلاً در گزارش اول انجام شد:
- `task_01_04` که به `task_10_06_game_manager...` ارجاع می‌داد به `task_10_05...` اصلاح شد
- الان هیچ ارجاع اشتباه باقی نمانده

### 14. شماره Task تغییر Main Scene اشتباه بود
**فیکس:** `task_01_01`:
- `TASK-10-06 will later change` → `TASK-10-05 will`
- الان صحیح: Main Scene در 10-05 به Hub تغییر می‌کند

### 15. تعداد Taskها اشتباه محاسبه شده بود (65 vs 58)
**فیکس:** جستجو شد، هیچ فایل فعلی ادعای 65 نمی‌کند (فقط در فایل اصلی بزرگ که منبع اولیه بود). گزارش اول این ناسازگاری را ثبت کرد. نیازی به تغییر کد نیست.

### 16. پیش‌نیاز TASK-04-01 با ساختار قفل‌شده هماهنگ نبود
**فیکس:** `task_04_01`:
- قبل: فقط `part_01_foundation fully completed`
- بعد: اضافه شد `part_03_health_damage fully completed and verified (per ADR-008 locked order)`

## نتیجه
- تمام 16 مورد بررسی و در سطح مستندات اصلاح شد
- هیچ فایل `Game/` یا کد ساخته نشد
- تمام ارجاعات فیزیک الان به ADR-014 با bitmask صحیح است
- تست‌ها lambda-unsafe ندارند
- Placeholder‌ها Polygon2D شدند
- قوانین CURRENT_TASK, collision comment, testing discipline شفاف شدند

## وضعیت فعلی
- موتور قفل: `v4.7.1.stable.official [a13da4feb]`
- ADR-014 مرجع صحیح
- TASK-02-04 / 03-03 composition pattern حفظ شد
- Projectile دیوار را هم حذف می‌کند
- LootPickup type-safe شد
- Patrol overshoot فیکس شد
- ItemDatabase typed dict شد
- TASK-09-03 الان تست committed دارد

آماده برای شروع `TASK-01-01` پس از تایید.
